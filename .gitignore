# macOS
.DS_Store
._*
.AppleDouble
.Spotlight-V100
.Trashes

# iCloud sync placeholders
*.icloud

# Editors
*.swp
*~
.idea/
.vscode/

# Secrets (defensive — none today, keeps it that way)
.env
.env.*
*.pem
*.key
credentials.json

# Internal evaluation / working reports — NOT part of the published Constitution.
# (Holds example secret-shaped strings; kept local so they never publish.)
report/

# Author's personal diary — never versioned, never published
AUTHOR-NOTES.md
